# VITE_SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
# VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# NOTE: Gemini and Hugging Face keys are configured by the end-user via the 
# "Developer Settings" page in the UI (BYOK model) and are stored in localStorage/Supabase.
# They are sent to the backend /api/* routes via request body/headers.
